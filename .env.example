# x402-fac — production environment variables
# Copy to .env and fill in values before running docker-compose.prod.yml

# Redis password (choose a strong random string)
REDIS_PASSWORD=changeme-use-a-strong-password
